tomatobot:
  loglevel: "trace"
  debug: false
  send_proxied_response_to_chat: true
  database:
    type: "sqlite"
    log_queries: false
    connection_string: "file:db.sqlite?cache=shared"
  modules:
    weather:
      polling_interval: 60s