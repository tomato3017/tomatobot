tomatobot:
  loglevel: "trace"
  debug: false
  database:
    type: "sqlite"
    log_queries: false
    connection_string: "file:db.sqlite?cache=shared"
  modules:
    weather:
      polling_interval: 60s